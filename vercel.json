{
    "version": 2,
    "builds": [
        {
            "src": "dist/main.js",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "handle": "dist/main.js",
            "src": "/(.*)",
            "methods": [ "GET", "POST", "PUT", "PATCH", "DELETE" ]
        }
    ]
}